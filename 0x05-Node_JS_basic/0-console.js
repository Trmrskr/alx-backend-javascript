/**
 * Logs a message to the consoel
 * @param {string} message - The message to log.
 * @returns {void}
 */

const displayMessage = (str_arg) => {
  console.log(str_arg);
}

module.exports = displayMessage;
