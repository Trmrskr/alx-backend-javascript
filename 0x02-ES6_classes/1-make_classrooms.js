import ClassRoom from './0-classroom';

/**
 * Create and return an array of classRoom objects
 */

export default function initializeRooms() {
  return [new ClassRoom(19), new ClassRoom(20), new ClassRoom(34)];
}
